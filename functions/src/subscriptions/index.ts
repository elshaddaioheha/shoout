/**
 * Subscriptions domain module — barrel export.
 *
 * Usage: import { catalog, entitlements, guards, lifecycle, storagePolicy } from '../subscriptions';
 * Or:    import { resolveEntitlements } from '../subscriptions/entitlements';
 */

export * as catalog from './catalog';
export * as entitlements from './entitlements';
export * as guards from './guards';
export * as lifecycle from './lifecycle';
export * as storagePolicy from './storagePolicy';
