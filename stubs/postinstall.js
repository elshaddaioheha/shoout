// Stub for @firebase/util's postinstall.mjs — not needed at runtime in React Native.
// Metro cannot resolve .mjs files; this empty shim prevents the bundling error.
export function getDefaultsFromPostinstall() {
  return {};
}
